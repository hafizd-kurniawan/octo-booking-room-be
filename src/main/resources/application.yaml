spring:
  application:
    name: booking-room
  datasource:
    url: jdbc:mysql://localhost:3306/octo_booking_room?useSSL=false&serverTimezone=UTC
    username: <username>
    password: <password>
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
  
